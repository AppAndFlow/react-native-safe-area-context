import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('SafeAreaExampleWeb', () => App);
AppRegistry.runApplication('SafeAreaExampleWeb', {
  rootTag: document.getElementById('root'),
});
